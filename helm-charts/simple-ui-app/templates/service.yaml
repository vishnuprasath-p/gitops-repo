apiVersion: v1
kind: Service

metadata:
  name: simple-ui-app

spec:
  type: {{ .Values.service.type }}

  selector:
    app: simple-ui-app

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}